{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment_ignore": "Docs/memory/md degisikliklerinde build skip. Exit 0 = no diff (exclude paths disinda) = Vercel build'i atlar. Code degisikliklerinde normal build. Oturum 12 Fluid Active CPU %75 olunca eklendi.",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':(exclude)docs/' ':(exclude)memory/' ':(exclude)*.md'",
  "crons": [
    {
      "path": "/api/cron/indexnow",
      "schedule": "0 8 * * 1"
    },
    {
      "path": "/api/cron/audit-report",
      "schedule": "0 7 * * 1"
    },
    {
      "path": "/api/cron/newsletter",
      "schedule": "0 6 * * 1"
    }
  ]
}
